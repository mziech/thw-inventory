server.port=${test.server.port}
local.server.port=${test.server.port}
cas.server.url=http://localhost:8000/apps/cas
cas.callback.url=http://localhost:${test.server.port}${server.servlet.context-path:}/login/cas
spring.datasource.url=jdbc:h2:mem:test;NON_KEYWORDS=USER;MODE=MySQL;DATABASE_TO_LOWER=TRUE
